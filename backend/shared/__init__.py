"""Shared Lambda utilities for Groundskeeper.

This package is bundled into each Lambda zip at the zip root (see
``_zip_handler_dir`` in ``deploy.py``), so handlers can import as
``from shared.foo import bar``.
"""
